<%= render "recipe_meal_form",
      recipe: @recipe,
      daily_log: DailyLog.today,
      products: @products,
      form_url: daily_log_meal_entries_path(DailyLog.today),
      form_method: :post %>
