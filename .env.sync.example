# Copy to .env.sync on your Mac (gitignored) if defaults don't work:
#   cp .env.sync.example .env.sync
#
# Tailscale hostname or IP for your Pi:
PI_HOST=pi@raspberrypi
# PI_HOST=pi@100.77.81.8
PI_PATH=/home/pi/silver-happiness
