# Copy to .env.production on the Pi (never commit the real file):
#   cp .env.production.example .env.production
#   nano .env.production
#
# Generate a secret:
#   bin/rails secret

RAILS_ENV=production
SECRET_KEY_BASE=replace_with_output_of_bin_rails_secret
